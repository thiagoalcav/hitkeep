# Import Test Fixtures

These fixtures are versioned for backend and dashboard import tests. Keep shared
test fixtures under `tests/fixtures` so they stay out of app `public` embed trees
and production runtime assets.
